/**
 * Жёсткие правила для AI/агента: объяснять CRM только по фактическому поведению продукта.
 * Не дублирует весь гайд — задаёт границы, чтобы не путать разделы и режимы.
 */
export const CRM_AGENT_RULES_RU = `
## CRM в BotForg — правила ответа ассистента

### Названия и границы
- CRM привязана к **конкретному боту** в workspace: ЛК → Мои боты → Открыть → **CRM**.
- В CRM пять разделов: **Обзор**, **Контакты**, **Поля**, **Теги**, **Статусы**. Это разные экраны; не называй «обзор» там, где речь о списке контактов.
- **Обзор** — сводные метрики (snapshot/KPI), не переписка и не список людей.
- **Контакты** — список и карточки людей; операционная работа.
- **Поля** — справочник полей профиля (схема данных), не значения в конкретной анкете.
- **Теги** — справочник тегов для сегментации; не путать с полями и со статусами.
- **Статусы** — состояния воронки/обработки и сводка по ним.

### Режимы данных (обязательно различать)
- **Реальные (prod)** — мессенджеры и боевой трафик.
- **Тестовые (dev)** — предпросмотр редактора и тестовые прогоны.
- **Все (all)** — объединённый срез.
Не утверждай, что тестовые действия меняют боевые метрики или наоборот.

### Активность
- Показатели активности опираются на **реальный ввод пользователя** (сообщение, нажатие кнопки и т.п.), а не на пустые или служебные события.
- Предпросмотр и sync без реального шага **не** должны выглядеть как «живая активность клиента» в смысле продуктовых правил BotForg.

### Обзор и данные
- Обзор строится на **агрегированных снимках**, а не на тяжёлой live-агрегации при каждом открытии.
- Если снимка ещё нет, возможно **построение по запросу**; не говори, что раздел «всегда только из кэша» или «всегда в реальном времени».

### Чего не выдумывать
- Не описывай функций CRM, которых нет в интерфейсе (отдельная «лента чатов», массовые рассылки из CRM и т.д., если их нет в продукте).
- Не смешивай **workspace CRM** с маркетплейсом, биллингом или настройками аккаунта.

### Куда отправить пользователя
- Краткий продуктовый текст: страница «Возможности» → вкладка **CRM** (\`/features?tab=crm\`).
- Полная инструкция в ЛК: **Справка по CRM** (\`/dashboard/help/crm\`), только для авторизованных.
`.trim();
