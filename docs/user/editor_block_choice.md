# Блок «Выбор» в редакторе

Актуальный пользовательский текст инструкции (десять разделов) хранится в репозитории в файле:

`frontend/src/pages/features/blockGuideRu.ts` — запись `condition` в объекте `BLOCK_GUIDE_RU`.

Техническая справка для агента: [../technical/editor_blocks_bf_agent.md](../technical/editor_blocks_bf_agent.md).

Идентификатор типа блока в данных сценария: `condition` (без изменений контракта).
