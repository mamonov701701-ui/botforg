# 🚀 БЫСТРЫЙ СТАРТ - BotForg

## ✅ ЧТО ГОТОВО

**Backend:**
- ✅ API endpoints для ботов, сценариев, команд
- ✅ Авторизация (email + OAuth)
- ✅ База данных SQLite
- ✅ Автосоздание главного сценария

**Frontend:**
- ✅ Визуальный редактор сценариев
- ✅ Drag & Drop блоков
- ✅ Автосохранение (каждые 30 сек)
- ✅ Модальное окно авторизации

---

## 🎯 ЗАПУСК (3 ПРОСТЫХ ШАГА)

### **Шаг 1: Запустите серверы**

```powershell
# Terminal 1 - Backend
cd C:\Users\mamon\botforg\backend
.\venv\Scripts\Activate.ps1
uvicorn main:app --reload --port 8001

# Terminal 2 - Frontend
cd C:\Users\mamon\botforg\frontend
npm run dev
```

### **Шаг 2: Войдите в систему**

1. Откройте http://localhost:5173
2. Нажмите **"Личный кабинет"** в шапке
3. Откроется модальное окно авторизации
4. Выберите вкладку **"Вход"** или **"Регистрация"**

### **Шаг 3: Используйте редактор**

1. После входа перейдите в **"Редактор"**
2. Нажмите **"Добавить блок"**
3. Выберите блок из библиотеки
4. Перетаскивайте блоки на холст

---

## 👤 УЧЁТНЫЕ ДАННЫЕ

### **Владелец платформы (уже создан):**
```
📧 Email:    mamonov701701@mail.ru
🔑 Password: Test123456!
👑 Роль:     owner
```

### **Демо пользователь:**
```powershell
# Создать демо пользователя:
cd backend && .\venv\Scripts\Activate.ps1 && python setup_demo.py
```
```
📧 Email:    demo@bot.ru
🔑 Password: Demo123!
👤 Роль:     moderator
```

### **Новый пользователь:**
1. Нажмите "Личный кабинет"
2. Выберите вкладку "Регистрация"
3. Заполните: Email, Логин, Пароль
4. Нажмите "Зарегистрироваться"

---

## 🔧 СБРОС ПАРОЛЯ

Если забыли пароль, выполните:

```powershell
cd C:\Users\mamon\botforg
python reset_password.py
```

Или создайте скрипт `reset_my_password.py`:

```python
import sqlite3
import sys
sys.path.insert(0, 'backend')
from security import get_password_hash

email = "ваш@email.com"
new_password = "НовыйПароль123!"
hashed = get_password_hash(new_password)

conn = sqlite3.connect('backend/botforg.db')
cursor = conn.cursor()
cursor.execute("UPDATE users SET hashed_password = ?, password_hash = ? WHERE email = ?", (hashed, hashed, email))
conn.commit()
print(f"✅ Пароль для {email} сброшен на: {new_password}")
conn.close()
```

---

## 🧪 ЧТО ПРОВЕРИТЬ

### **1. Авторизация**
- ✅ Модальное окно открывается при клике на "Личный кабинет"
- ✅ Вход по email/паролю работает
- ✅ OAuth через Google/Яндекс/Mail.ru

### **2. Редактор сценариев**
- ✅ Добавление блоков (кнопка "Добавить блок")
- ✅ Перетаскивание блоков на холсте
- ✅ Блоки НЕ исчезают после перетаскивания
- ✅ Соединение блоков (drag от точки к точке)

### **3. Сохранение**
- ✅ Ручное сохранение (кнопка "Сохранить")
- ✅ Автосохранение каждые 30 секунд
- ✅ Уведомления о сохранении

---

## 🐛 TROUBLESHOOTING

### **Ошибка 401 Unauthorized:**
Не авторизованы. Войдите через "Личный кабинет".

### **"Пользователь с таким email существует":**
Этот email уже зарегистрирован. Переключитесь на вкладку "Вход".

### **Забыли пароль:**
Используйте скрипт `reset_password.py` (см. выше).

### **Редактор исчезает после drag:**
Обновите страницу (F5). Баг исправлен в последней версии.

### **Сценарии не загружаются:**
1. Откройте консоль браузера (F12)
2. Проверьте Network tab
3. Если 401 - войдите в систему
4. Если 500 - перезапустите backend

---

## 📊 СТАТУС ГОТОВНОСТИ

| Компонент | Статус |
|-----------|--------|
| Backend API | ✅ 100% |
| База данных | ✅ 100% |
| Авторизация | ✅ 100% |
| Визуальный редактор | ✅ 100% |
| Drag & Drop | ✅ 100% |
| Автосохранение | ✅ 100% |

---

## 🎯 СИСТЕМА ГОТОВА К ИСПОЛЬЗОВАНИЮ!

Все компоненты работают. Регистрируйтесь, создавайте ботов и сценарии! 🚀
